/* Biblioteka standardowych wejść / wyjść - zawiera funkcję printf*/
#include <stdio.h>

/* Funkcja main - tu dzieje się większość operacji 
 * int - typ zmiennej zwracanej przez funkcję [Na razie to nas nie obchodzi]
 * void - typ (pusty) argumentu przyjmowanego przez funkcję [Na razie to nas nie obchodzi]*/
int main(void){

    /* funkcja wyświetlająca w konsoli łańcuch znaków */
    printf("Hello IMPULS candidate\r\n");

    /* wartość zwracana przez funkcję main [na razie to nas nie obchodzi]*/
    return 0;   
}